// This is startpoint of Frontend single page application(SPA)

import React, { Component } from 'react';
import { render } from 'react-dom';
import Root from './root';

render(<Root />, document.getElementById('root'));
